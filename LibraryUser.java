public class LibraryUser {
    // ArrayList<Book> borrowedBooks

    // Needs to iterate through collection for requested book and borrow it
    /* public void borrow(LibraryCollection collection) throws BookNotAvailableException, BookNotAvailableException Book {
        if (!book.isAvailable()) {
            throw new BookNotAvailableException("Book not available");
        } else if ( )
    }; */
}